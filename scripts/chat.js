(function initializeChatComponent(component) {
	// Attach the updateChatUI function to handle UI updates when a friend is selected
	// component.updateChatUI = function(friendName) {
	// 	console.log("hh" + friendName);
	// 	loadComponent2('chat-component', friendName);
	// };
})(this);